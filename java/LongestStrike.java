import java.util.Scanner;

/**
 * This problem's task can be found at : https://codeforces.com/problemset/problem/1676/F
 */

public class LongestStrike
{
    public static void main(String[] args)
    {
        try(Scanner myObj = new Scanner(System.in))
        {
            int tests = myObj.nextInt(), n;

            boolean equalsFail, result;

            long[] target, temp;

            for(int i = 0; i < tests; i++)
            {
                
            }

            // Main algorithm:

            // Sort the input

            // Get the next consequtive* element
            // Check if lastIndex() - firstIndex >= k
            // if not remember the pair <strikeStartm, strikeEnd>
            // if next pairs are bigger reassign
            // if not skip.
        }
    }
}
